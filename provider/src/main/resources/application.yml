spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
      filter:
        enabled: false
  application:
    name: p1
server:
  port: 8083
management:
  endpoint:
    web:
      exposure:
        include: '*'